<%= form_with(model: cert, local: true) do |form| %>
  <% if cert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cert.errors.count, "error") %> prohibited this cert from being saved:</h2>

      <ul>
      <% cert.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>

  <% end %>

    <div class="form-group">
        <%= form.file_field :upload, class: 'form-control-file' %>
    </div>


  <div class="form-group pt-3">
    <%= form.submit "submit", {class: "btn btn-primary"} %>
  </div>
<% end %>

<form>

</form>
